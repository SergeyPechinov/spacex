export type TLaunchpad = {
  details: string,
  full_name: string,
  id: string,
  latitude: number,
  launch_attempts: number,
  launch_successes: number,
  launches: string[],
  locality: string,
  longitude: number,
  name: string,
  region: string,
  rockets: string[],
  status: string,
  timezone: string
}